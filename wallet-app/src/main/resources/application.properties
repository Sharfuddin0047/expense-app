spring.application.name=${APP_NAME:wallet-app}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:wallet_app}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:Sharfu@8826}

spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.show-sql=${SHOW_SQL:true}

server.port=${SERVER_PORT:1011}

springdoc.api-docs.enabled=${API_DOCS_ENABLED:true}
springdoc.swagger-ui.enabled=${SWAGGER_ENABLED:true}

management.endpoints.web.exposure.include=health,info,metrics
